function init(self)
	msg.post(".", "acquire_input_focus")
	self.state = require 'main/state'
	self.old_state = self.state


	--self.text_node = label.get_node("text")
	self.node_address = "#label";
	label.set_text(self.node_address, "New text")
    
end

function update(self, dt)
	label.set_text(self.node_address, self.state.field[1])
end